<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="styles.css">
  <title>Document</title>
</head>
<body>
  <h1>Welcome fellow traders</h1>

  <% if params[:stock] != nil && params[:stock] != '' %>
    <h1><%= @stock %></h1>
  <%end%>

  <% if params[:stock] == nil || params[:stock] == '' %>
    <h2><%= "In the address bar, put in /?stock=\"the stock code that you want to see\" and it will display below"%></h2>
  <%end%>

  <% if params[:stock] != nil && params[:stock] != '' %>
  <h2>The current stock price for <%= @stock%> is: <%= @stock_price %></h2>
  <%end%>

  <% if params[:stock] != nil && params[:stock] != '' %>
  <h2>The day range is: <%= @day_range %></h2>
  <%end%>

  <% if params[:stock] != nil && params[:stock] != '' %>
  <h2>The daily volume is: <%= @volume %></h2>
  <%end%>

</body>
</html>
